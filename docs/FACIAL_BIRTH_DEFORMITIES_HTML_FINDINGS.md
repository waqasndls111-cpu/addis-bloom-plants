# Facial Birth Deformities HTML Findings

The supplied outer HTML for `/facial-birth-deformities/` confirms these durable extraction targets:

- `body` includes `page-id-4051` and `elementor-page-4051`.
- Canonical URL is `https://statenislandoralsurgery.us/facial-birth-deformities/`.
- The active header path is under `Services > Facial Birth Deformities`.
- The page H1 is `Staten Island Facial Birth Deformities`.
- The hero image uses `SIOMS-3.jpg` with alt text `Woman holding baby with Facial Birth Deformities in Staten Island`.
- The secondary body image uses `15.jpg` with alt text `Woman holding baby with cleft lip`.
- The visible child-page links are rendered as Elementor icon-list links and should be located by link text and href, not generated menu IDs.
