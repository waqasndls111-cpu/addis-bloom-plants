# Cleft Lip & Palate Surgery HTML Findings

The supplied outer HTML for `/cleft-lip-palate-surgery/` confirms these durable extraction targets:

- `body` includes `page-id-4235` and `elementor-page-4235`.
- Canonical URL is `https://statenislandoralsurgery.us/cleft-lip-palate-surgery/`.
- The active header path is under `Services > Facial Birth Deformities > Cleft Lip & Palate Surgery`.
- The page H1 is `Staten Island, NY Cleft Palate & Cleft Lip Repair`.
- The hero image uses `SIOMS-8.jpg` with alt text `Baby with cleft palate in NY`.
- The secondary body image uses `Staten-Island-Oral-and-Maxillofacial-Surgery.jpg` with alt text `Baby with cleft lip and cleft palate laying down`.
- The before-and-after carousel contains 21 unique upload images from `4-5.jpg` through `25.jpg`, plus duplicated Swiper slides for looping.
- The video widgets include YouTube IDs `kfD-g8M4JBU`, `hKt6mYy3YrQ`, and `iLOHJO0Fb6Y`.
- The Lottie widget loads `21-04-15-Different-age-and-gender-groups-line-2.aep_.json`.
- FAQ questions are rendered as Jet Accordion toggle labels, so tests should use the visible question text instead of generated IDs such as `jet-toggle-control-1091`.
